# Production build-time configuration.
# NEXT_PUBLIC_* vars are inlined into the client bundle by Next.js at build time.
# The backend URL is public (browsers call it directly) — this is not a secret.
NEXT_PUBLIC_API_URL=https://bussiness-twin-ai.onrender.com/api/v1
